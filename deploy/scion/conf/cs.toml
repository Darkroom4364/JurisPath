[general]
id = "cs-1"
config_dir = "/etc/scion"

[log.console]
level = "info"

[trust_db]
connection = "/var/lib/scion/trust.db"

[beacon_db]
connection = "/var/lib/scion/beacon.db"

[path_db]
connection = "/var/lib/scion/path.db"

[beaconing]
origination_interval = "5s"
propagation_interval = "5s"
registration_interval = "5s"

[api]
addr = "0.0.0.0:30452"
